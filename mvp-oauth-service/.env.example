# MVP OAuth Service Environment Configuration
# Copy to .env and customize for your deployment

# JWT Secret for token signing (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server configuration
PORT=3000

# Logging level (trace, debug, info, warn, error)
RUST_LOG=info

# Optional: Custom bind address (default: 0.0.0.0)
BIND_ADDRESS=0.0.0.0

# Security settings
CORS_ORIGIN=*

# Production deployment notes:
# 1. Generate a strong JWT_SECRET: openssl rand -base64 32
# 2. Use HTTPS in production with proper SSL certificates
# 3. Configure CORS_ORIGIN to specific domains in production
# 4. Enable proper logging and monitoring
# 5. Use a secrets management system for sensitive values